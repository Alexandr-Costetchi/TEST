# 📋 ИНСТРУКЦИЯ ПО ВНЕДРЕНИЮ

## Что было сделано?

Создана **новая модульная архитектура** для системы тестирования:

✅ Разделены файлы на модули:
- `Код.gs` — логика и конфигурация
- `generateTestHTML.gs` — генерирование интерфейса  
- `init_questions.gs` — загрузка данных вопросов

✅ Вопросы теперь в **Google Sheets**, а не в коде

✅ **Универсальный движок** работает с любым ID теста (32, 33, 34, ...)

✅ Поддержка **100+ тестов** без изменения кода

---

## 🚀 ПОШАГОВОЕ ВНЕДРЕНИЕ

### Шаг 1: Скопировать код в Google Apps Script

Откройте Google Sheet:
```
https://docs.google.com/spreadsheets/d/1nP5GTP9sg8yqgOetbytOTYu5ahFfUOgVjw_NDl5Mi_A/
```

**Инструменты → Редактор Apps Script**

#### Файл 1: `Код.gs` (основной файл)

1. Откроем файл `src/gas/Код.gs` из проекта
2. Скопировать ВСЕ содержимое
3. В Google Apps Script удалим старый код и вставим новый

#### Файл 2: `generateTestHTML.gs` (новый файл)

1. В Apps Script: **+ (плюс)** → **Создать файл** → назвать `generateTestHTML`
2. Скопировать содержимое из `src/gas/generateTestHTML.gs`

#### Файл 3: `init_questions.gs` (новый файл)

1. В Apps Script: **+ (плюс)** → **Создать файл** → назвать `init_questions`
2. Скопировать содержимое из `src/gas/init_questions.gs`
3. ⚠️ **ВАЖНО**: Заменить плейсхолдеры на реальные вопросы из документа:
   ```javascript
   // ВРЕМЕННО (заглушка):
   [33, 1, "[ВСТАВИТЬ ТЕКСТ ВОПРОСА 1]", "Опция А", "Опция Б", ...
   
   // ПРАВИЛЬНО (пример):
   [33, 1, "Какое из этих понятий...", "Определение 1", "Определение 2", ...
   ```

---

### Шаг 2: Инициализировать листы

Откройте **Google Sheet** → меню **🧪 Тесты** → **📋 Инициализировать листы**

Это создаст:
- Лист **"Вопросы"** — для хранения вопросов
- Лист **"Тесты"** — для метаданных тестов
- Лист **"Результаты теста"** — для сохранения ответов студентов

---

### Шаг 3: Добавить тест 33 (26 вопросов)

#### Способ A: Через Google Sheet UI (ВРУЧНУЮ)

1. Откройте лист **"Тесты"**
2. Строка 2: `33 | Тест 3.4, 3.6 | 26 | 45 | Выберите все правильные ответы`
3. Откройте лист **"Вопросы"**
4. Добавьте 26 строк с вопросами из документа

#### Способ B: Через скрипт (АВТОМАТИЧЕСКИЙ)

1. Отредактируйте `init_questions.gs`:
   - Замените **все 26 вопросов** (сейчас там заглушки)
   - Вставьте текст, опции, правильные ответы и комментарии
2. В Google Apps Script: **Выбрать функцию** → `addTest33Questions`
3. **Запустить** (выполнить)
4. Функция автоматически добавит вопросы в лист

---

### Шаг 4: Опубликовать тест

В Google Apps Script: **Развернуть** → **Новое развёртывание** → **Web app**

Параметры:
- Выполнять как: **ваш аккаунт**
- Доступно: **Любые пользователи**

Получите ссылку вида:
```
https://script.google.com/macros/s/[ID]/exec?test=33
```

---

## 📌 КАК Я МОГУ ПОМОЧЬ?

Я могу:

✅ **Редактировать файлы** прямо из проекта (используя Bash и инструменты Claude)

✅ **Проверять синтаксис** перед развертыванием  

✅ **Добавлять новые функции** (например, экспорт в PDF, отправку email)

✅ **Масштабировать систему** для дополнительных тестов

✅ **Отладить ошибки** если что-то не работает

---

## 🔧 КОМАНДЫ ДЛЯ РАБОТЫ СО МНОЙ

### Редактирование файла
```bash
# Я могу прочитать и отредактировать любой файл
# Просто скажи мне что именно нужно изменить
"Добавь все 26 вопросов теста 33 в init_questions.gs"
```

### Добавление новых файлов
```bash
# Создам новый файл с кодом
"Создай файл test_engine.gs для...?"
```

### Проверка кода
```bash
# Проверю синтаксис и совместимость
"Проверь правильность JSON в test_33.json"
```

### Git операции
```bash
# Буду вести версионирование
git status
git log
git diff
```

---

## ⚠️ КРИТИЧЕСКИЕ ТОЧКИ

**Перед каждым развертыванием:**

1. ✅ Проверить что все 26 вопросов добавлены
2. ✅ Проверить формат "Правильные ответы" (JSON: `[1,3]`)
3. ✅ Проверить что нет синтаксических ошибок
4. ✅ Протестировать тест перед публикацией

**Если что-то сломалось:**

1. Откройте Google Apps Script → посмотрите консоль (View → Logs)
2. Скопируйте ошибку и скажите мне
3. Я найду и исправлю проблему

---

## 📧 СЛЕДУЮЩИЕ ШАГИ

1. **Отправьте мне 26 вопросов** из документа "3.4, 3.6. Домашнее задание. Часть 1.docx"
2. **Скажите в какой формат** вы хотите их видеть (JSON, CSV, таблица)
3. Я **автоматически заполню** все файлы и **проверю** перед развертыванием

Или:

- **Вставьте вопросы вручную** в лист "Вопросы" через Google Sheet UI
- Запустите меню **🧪 Тесты → ➕ Добавить тест 33**
- Получите ссылку через **🔗 Получить ссылку на тест 33**

---

**Готово!** Система готова к работе. 🎉
